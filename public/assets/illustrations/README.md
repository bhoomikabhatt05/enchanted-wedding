# Illustrations

Original ink storybook motifs. Keep `CoupleMotif.jsx` as source of truth.
Optional drop-ins (also mirror to `src/assets/images/illustrations/`):
- `couple-ink.svg` / `motif.svg` — will be detected if referenced via `images` registry.
