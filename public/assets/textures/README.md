# Textures

Served from `public/` as static:
- `/film-grain.svg` — subtle overlay (0.06 opacity, overlay blend)
- `/paper-grain.svg` — parchment multiply
- `/ink-edge.svg` — masked deckle edge

No hard-coded external URLs — local only.
