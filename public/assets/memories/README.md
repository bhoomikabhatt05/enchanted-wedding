# Memories Rail

8 slots, also mirror to `src/assets/images/memories/`:
- `memory01` / `memory-01` through `memory08` / `memory-08`

Rail shows 01/08 counters and caption sync. Placeholder `ArtFrame` shows if missing.
